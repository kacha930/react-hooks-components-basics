import React from "react";

function Article() {

  //The function has a return value of JSX,
  // which is our way of telling React "Hey, when you want to put this component on the DOM, here is what it should become!"
  return (
    <div>Dear Reader: Bjarne Stroustrup has the perfect lecture oration.</div>
  );
}

export default Article;

//  Now let's make a component to display a single user's comment: >>>>

